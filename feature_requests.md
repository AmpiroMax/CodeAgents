<!--
Маркеры статуса (на уровне отдельных пунктов):
[+]  — сделано и протестировано
[~]  — частично сделано / есть инфраструктура, но не end-to-end
[-]  — не сделано
-->

Пачка фичреквестов 1
Сделано: да (после Phase 1)

[+] 1) добавить поиск от ollama, пусть он в параллель вместе с яндексом работает. По очереди то один, то другой. Если кто-то из них выдал ошибку, то другого дренуть, если оба падают, то переходим на других представителей. Ключ для ollama для работы с их апи я уже в config закинул даже
    → Phase 1 эпик A. Round-robin `[ollama, yandex]` + fallback на старую цепочку (searxng/jina/brave/...).
[~] 2) Когда модель запускается и ещё не начала отвечать, загружается в гпу, нужно так и писать пользователю об этом, а не говорить, что модель думает
    → Phase 1 эпик B. В StatusBar теперь показывается «model: X · loading…» пока модели нет в `/api/ps`. Логика «думает vs грузится» в timeline-сообщениях ещё разводится не везде — может потребоваться доработка таймлайна.
[+] 3) Обязателен стриминг сообщений и ВАЖНО стриминг вызова команд. В документации про стриминг и про тулы в папке docs/ollama точно можно найти информацию как это сделать. Модели точно это поддерживают.
    → стриминг текста и tool_call'ов уже работает (см. AgentCore.complete_chat_stream + tool_call_start события).
[+] 4) Когда команда выполняется, надо это отображать, как процесс, показывать крутящееся что-нибудь, чтобы юзер видел, что мы не зависли и модель что-то вызвала.
    → tool-cards с running/done/error статусами в ChatTimeline.
[+] 5) нормальное профилирование затрат гпу/цпу/опретивы. Графики в реальном времени о затратах. Логирования статистики использования и сохранение её, для отображение при дальнейших запусках. Логирование сообщений от ollama и действий внутри нашего mcp
    → Phase 1 эпик B. `metrics_sampler` (1Hz, ring 600pts), `.codeagents/metrics.jsonl` ротация, `GET /metrics/stream` NDJSON, GUI MetricsPanel со sparkline'ами CPU/RAM/GPU/Ollama. MCP-логи остаются в `.codeagents/audit.jsonl` через `inference_log.py` / `request_log.py`.

Пачка фичреквестов 2
Сделано: да

[+] 1) Plan-режим, plan-banner справа, build/continue/dismiss, command palette → Plans, persist в `<chat>/plans/`. patch_plan / mark_step / list_plans / create_plan все рабочие.

Пачка фичреквестов 3
Сделано: да

[+] 1) `registry/system_prompts.json` + `system_prompts.py` резолвят addendum под (model, mode). `registry/model_params/<model>.toml` для параметров генерации.

Пачка фичреквестов 4.1
Сделано: частично (Phase 1 закрыл foundation; авто-RAG в контекст — Phase 2)

[~] 1) Надо уж продумать какие тулы нужны для поискового агента
    → Phase 2. Дизайн уже наметили в `docs/papers/INDEX.md` (раздел D). Тулы: `web_search`, `web_fetch` (есть), `kg_extract`, `kg_query`, `python` (нет), `make_subplan` (есть как `create_plan`/`patch_plan`).
[+] 2) Надо перелопатить поиск семантический по коду / рабочей / текущей папке. Надо запустить эмбеддера текстов всех файлов (вопрос об .codeagentsignore чтобы ну не все индексировать). Чтобы агент, когда поиск делал, по его запросу по эмбедингам искалось и ещё как он сам захочет. Это поможет в анализе статей и написании большого кода. Эмбедить надо чанками по N строк, пусть N=20 будет. Эмбедерра с ollama скачаю и пусть он фоном в воркспейсе всё индексирует и не мешает работать агенту целевому. Надо будет это серьезно проверить
    → Phase 1 эпики D + E. 20-line чанки + overlap=4, `.codeagentsignore`, `embeddinggemma:300m`, фон-индексер каждые 30s, тул `search_code(query, k, scope)`, palette «Reindex workspace». sqlite-vec лежит в зависимостях, mirror в vec_chunks работает где extension загружается.
[+] 3) надо почитать какие есть статьи по теме построения раг агентов, поисковых агентов
    → `docs/papers/INDEX.md` собран. 6 abstract-ов закачаны как `<arxiv_id>.md`. Скрипт shallow-clone'ов: `thirdparty/papers/CLONE.sh`.
[~] 4) надо следить за объемом контекста, который модель занимает. Сейчас если мы весь диалог зажимаем, то время ожидания генерации сильно быстро растет. Надо Делать раг по чату. Эмбеддить сообщения юзера и ответы ассистента, по чанкам (вопрос какого размера). У нас есть модель embeddinggemma:300m. Пусть она на фоне занимается эмбеддингом и текстов кода и реплик в чате. Пусть на фоне всегда крутиться тоже в оллама и не останавливается
    → Phase 1 эпик F: `chat_rag.py` + `recall_chat` тул + per-chat `<chat>/embeddings.sqlite3` + фон-эмбеддинг новых сообщений после каждого `StreamDoneEvent`. **НО:** автоподстановки в контекст и бюджета 90% контекста — пока нет. См. Phase 2 P2-A.
[~] 5) надо поправить текущий поиск, точно чтобы был поиск через ollama. надо чтобы агенту возвращались распаршенные страницы сайтов в формате md. Надо чтобы он мог также получить прям сырую страницу. Надо как-то маленьким моделям подсказать, что они могу по ссылкам на страницах сайтов переходить. Я хочу тобы модели могли по гитхабу прыгать и код прям с гитхаба рассматривать
    → Phase 1 эпики A + C: ollama search есть, web_fetch v2 возвращает `markdown` + `raw_html` + `links`, провайдер `ollama` через `/api/web_fetch` подключён. В description тула написано про переход по `links[]`. Надо проверить на маленьких моделях, попадаются ли они на навигацию по ссылкам — это уже эмпирика.
[+] 6) эмбеддинги реплик в чате сохраняем в папку с чатов в папку chat_embeddings, там в sql или другом либо векторном формате надо сохранять, чтобы быстро можно было искать и пополнять, эмбеддинги кода директории в которой работаем (процесс индексации), сохраняем в папку .codeagents в папку index, там тоже веркторная бд и тд. Надо продумать как на код релевантный выцеплять? Ну тут явно надо search модифицировать.
    → Phase 1: чат-эмбеддинги в `<chat>/embeddings.sqlite3`, код в `.codeagents/index.sqlite3` (с опциональным `vec_chunks` через sqlite-vec). Тул `search_code` использует семантический + лексический поиск.

Пачка фичреквестов 4.2
Сделано: частично (web_fetch — да; PDF и search-mode + KG — Phase 2/3)

[+] 1) возможно надо вернуть ф-цию web_fetch но вопрос для чего она была нужна? чисто скачать или сырую страницу получить
    → Phase 1 эпик C. Возвращает и `markdown`, и `raw_html`, и `links[]`, и `cleaned`. Тул зарегистрирован как READ_ONLY.
[-] 2) надо добавить ф-цию для парсинаг pdf файла. Как сейчас агенты / модели парсят пдфки? Мне надо, чтобы агент умел читать спокойно пдф файлы. Картинки можно пока игнорировать, но намеки на обработку изображений уже иметь
    → Phase 2 P2-D. Подходящий стек — `pypdf` (текст) + `pdfplumber` (таблицы) + опц. `unstructured` для смешанного контента. Превратить в тул `read_pdf(path, page_range?)`.
[-] 3) надо добавить ф-ции явно, думаю для поискового агента и раг агента это важно и полезно, создания графов свзяенй информации. Надо добавить режим в CA - search agent - розового цвета, чтобы он по какой-то теме длительно искал в инете информацию, структурирвоал и пополнял граф знаний. Переодически планировал что ему делать. Планировал сразу же какие в начале действия ему сделать. Как только их выполнял, прикидывал сколько ему ещё и чего дальше искать. Чтобы он в конце каждого плана оценивал надо ли ещё план по поиску создавать или нет. И как только все планы бы были вполнены, все они в папке с чатом будут лежать в планах. Результат поиска будет в папке search_results лежать, там папки с uuid для этого поиска. в этой папке граф знаний (вопрос в каком формате) и результат поиска - отчет в md формате.
    → Phase 2 P2-B + P2-C. Опираемся на LightRAG (C.2) + STORM outline (D.1) + Search-R1 цикл (D.2). См. `docs/papers/INDEX.md`.
[-] 4) У поискового агента должны быть ф-ции по работе с питоном, для доп аналитики. Ф-ции поиска, обязательно. Возможность читать пдф-ки. CRUD для работы с графом знаний
    → Phase 2. Добавить тул `python_eval(code)` (READ_ONLY sandbox с timeout), kg_* CRUD (`kg_add_node`, `kg_add_edge`, `kg_query`, `kg_export_md`).
[-] 5) У агентов после поиска будут ф-ции по чтению графа знаний
    → Phase 2 (вместе с пунктом 4).
[-] 6) важно результаты поиска как-то тоже индексировать, чтобы агент всегда мог почитать что он там искал что думал
    → Phase 2. Папка `search_results/<uuid>/` + reuse `WorkspaceIndexer` для эмбеддинга самих отчётов.
[-] 7) в командную паллетку надо добавить как с рассмотрением планов, рассмотрение результатов deep search
    → Phase 2 (после реализации search-mode).

Пачка фичреквестов 5
Сделано: нет (запланировано на Phase 3)

[-] 1) убедиться, у нас вообще че получилось? мы редактор сделали? мы mcp сделали? что у нас вышло то? смысл в этом есть?
    → Phase 3. Документ `docs/STATE_OF_THE_PROJECT.md` со списком фич + бенчмарки.
[-] 2) как бы этот весь код опубликовать, надо документацию подготовить
    → Phase 3. README, CONTRIBUTING, examples/, скриншоты.
[-] 3) надо поправить то как в модель в чате при attach подается картинка. Она должна не просто путем до картинки подаваться, а прям вижн контентом и потом отправляться в модель. Важно, что не все модели умеют видить картинки. Пусть таким моделям картинка не подается, мы тут ничего поделать не можем. Также нужно, чтобы могли подаваться не 1 а несколько прикрепленных картинок
    → Phase 2 P2-D (vision). Использовать OpenAI-compat `image_url` контент-блоки (Ollama OpenAI-compat умеет с llava/qwen-vl/gemma3); фильтровать по `model.capabilities` из `registry/model_params`.

Пачка фичреквестов 6
Сделано: да

[~] 1) расложить проект по папкам, это уморительно, что /Users/ampiro/programs/CodeAgents/src/codeagents/tools_native/code.py в 3к строк уже. Там же ф-ции за разные вещи отвечающие лежат. Их можно разбить по папкам структурно
    → начат: `tools_native/rag.py` теперь содержит `search_code` + `recall_chat`, `code.py` re-импортирует. Полное расщепление web/file/shell-кластеров оставлено на отдельную итерацию (большой риск без юнит-тестов на каждый под-модуль).
[+] 2) Оконшко с метриками не закрывается на esc надо крестик нажимать. Надо добавить закрытие по esc
    → MetricsPanel слушает ESC и закрывается через `onClose`.

---

## Pack 7 — slim tool surface  [+]

- Видимых модели тулов: **19** (~3.7k tokens) вместо 30+ (~7.5k tokens).
- Активные: `read_file`, `write_file`, `edit_file`, `glob_files`, `grep`, `ls`,
  `pwd`, `rm`, `bash`, `web_search`, `web_fetch`, `cd`, `change_workspace`,
  `create_plan`, `patch_plan`, `mark_step`, `list_plans`, `search_code`,
  `recall_chat`.
- Скрытые (`enabled=False`, остаются доступны через прямой API/тесты):
  `cat`, `head`, `tail`, `wc`, `mkdir`, `mv`, `create_file`, `list_directory`,
  `search`, `propose_patch`, `safe_shell`, `shell` (alias bash), `run_python`,
  `python_module`, `flake8`, `pip_install`, `conda_*`, `git_diff`, `git_status`,
  `run_tests`, `curl`, `docs_search`.
- `bash` принимает любые команды, кроме содержащих `rm` — удаления только
  через отдельный `rm`-тул с явным подтверждением.
- Описания тулов в `config/tools.toml` сжаты до 1–3 строк + 1–2 примеров.

## Phase 2 эпики (предложение)

- **P2-A — Context budget + chat-RAG auto-recall**
  Считать токены `system + tools + history` на старте турна. Если >0.9·context_window:
    1. ужать набор тулов (per-mode + lazy unload неиспользованных);
    2. суммаризация старых сообщений в 1 system note;
    3. на каждый user-турн дёргать `recall_chat(top_k=3)` и подмешивать как hidden block.
- **P2-B — Search mode + KG (LightRAG-based)**
  Добавить mode=`search`, тулы `kg_extract / kg_add_node / kg_add_edge / kg_query`, отдельная subplan-loop.
- **P2-C — Deep-research outline writer (STORM-style)**
  Outline-driven отчёт, итерации planning + retrieval до критерия остановки.
- **P2-D — PDF + multi-image vision**
  `read_pdf` тул, image_url content-блоки в attachments, capability-фильтр.
